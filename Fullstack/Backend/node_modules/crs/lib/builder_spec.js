var Builder = require("crs").Builder;
var Domain = require("crs").Domain;

var builder = new Builder();

builder.addAggre(Aggre01)
			.addAggre(Aggre02)
			
			.addService(service01)
			.addService(service02)
			
			.addCommandHandle(commandHandle01)
			.addCommandHandle(commandHandle02)
			
			.on(AggreType,id,eventName,eventHandle01)
			.on(AggreType,id,eventName,eventHandle02);
			
builder.linkDB(db);

var domain = new Domain(builder);