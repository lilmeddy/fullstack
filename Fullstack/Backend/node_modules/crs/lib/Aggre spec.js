module.exports = {
	typeName:"MyAggre",
	initData:function(arg01, ...... ){
		......
		return data;  // data is json object , cann't have function object.
	},
	methods:{
		fun01:function(){
		
			// read data
			var name = this.data("name");
			
			// change data
			this.$data(k,v);
			
			// aggre operate
			var AggreType = this.$getAggre(typeName);
			var newAggObj = new AggreType({name:"leo"});
			AggreType.get("id002",function(obj){
				
			});
			
			// service operate
			var service = this.$getService("serviceName");
			service(args);
			
			// publish event.
			this.$publish("change name",name);			
			
		},
		......
	}
}